<!DOCTYPE html>
<html>
<head>
  <title>WanikaniTestingapp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <script src="http://code.jquery.com/jquery.js"></script>
  <div class="container">
    <% if !notice.nil? %><p class="notice"><%= notice %></p> <% end %>
    <% if !alert.nil? %><p class="alert"><%= alert %></p><% end %>
    <h1>WaniKani Test App</h1>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">
            <a class="row">
              <a class="active"><%= link_to 'Home', '/' %></a>
              <a>Something</a>
              <a><%= link_to current_user.email, user_path(current_user.id) %></a>
            </a>
          </a>
        </div>
      </div>
    </nav>
    <%= yield %>
  </div>
</body>
</html>
